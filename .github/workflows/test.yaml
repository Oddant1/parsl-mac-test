name: Run Test

on:
  workflow_dispatch:

jobs:
  run-test:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deps
        run: pip install parsl

      - name: Run test
        run: python test.py